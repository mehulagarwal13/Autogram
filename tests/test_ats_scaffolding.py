"""MOVED to automation/tests/test_ats_scaffolding.py — see MIGRATION.md.

The ATS-automation scaffolding it tests now lives under `automation/`, not
`app/`, so this copy (which imported `app.ats.*`) is stale. Inert placeholder
(no imports, no test collection) — could not be deleted (no shell/disk space
this session). Safe to remove manually; pytest will skip it (no test_*
functions defined)."""
